# Credentials — NEVER commit this
config.php
 
# Session cookie file(s)
etimologio_cookies.txt
.etimologio_cookies.txt
.cookies/
/tmp/etimologio_cookies.txt

# Local SQLite data (payments, ledgers, customer notes)
.localdata.sqlite
.localdata.sqlite-*

# Scheduler single-instance lock
.scheduler.lock
 
# OS & editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
 

# Local dev launch config (machine-specific paths)
.claude/

# Encryption key (NEVER commit)
.enckey

# Μυστικά deployment — ΠΟΤΕ στο git
.env

# Ηχογραφήσεις για την εκπαίδευση φωνής: τα WAV ΔΕΝ μπαίνουν στο repo
# (εκατοντάδες MB). Οι λίστες ανάγνωσης, ναι.
training/voice/readlist/**/*.wav
training/voice/readlist/wav/
training/voice/dataset/

# Παράγωγο σύνολο εκπαίδευσης: το φτιάχνει το check_datasets.py --export
# από τα τρία JSON, οπότε δεν έχει λόγο να ζει στο repo (1,4 MB διπλά).
training/voice/etimologio_sft.jsonl

# Το training/voice τρέχει scripts επιτόπου — τα .pyc δεν έχουν λόγο να ζουν στο repo.
training/voice/__pycache__/
