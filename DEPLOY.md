# Στήσιμο του e‑Τιμολόγιο Pro server (home server / VPS)

Αυτό το branch (`deploy/server`) έχει τα deployment αρχεία **στη ρίζα**, ώστε το
Coolify (ή ένα σκέτο `docker compose up`) να τα βρει χωρίς καμία ρύθμιση. Ο
φάκελος `desktop/` (η εφαρμογή υπολογιστή) υπάρχει κι εδώ γιατί είναι το ίδιο
repo, αλλά **δεν μπαίνει στο image** — το `.dockerignore` τον αποκλείει μαζί με
τα `docs/` και `.git/`.

Ένας container σερβίρει **και τα δύο μισά** του προϊόντος:

- το **web UI** (`app.php`) που χρησιμοποιούν οι πελάτες‑επιχειρήσεις από browser
- το **JSON API** που χρησιμοποιεί η εφαρμογή υπολογιστή σε λειτουργία thin client

Έτσι ο λογιστής και οι πελάτες του δουλεύουν πάνω στα **ίδια δεδομένα**.

```
 Υπολογιστής λογιστή                    Home server
 ┌───────────────────┐                  ┌────────────────────────────────┐
 │ Downloader        │                  │ cloudflared ──► Coolify proxy  │
 │ e‑Τιμολόγιο Pro   │ ── HTTPS ───────►│      │                         │
 └───────────────────┘                  │ etimologio (αυτό το image):8090│
 Πελάτες ── HTTPS (browser) ───────────►│ Postgres  +  volume → /data    │
                                        └────────────────────────────────┘
```

> 🔗 Οι ρυθμίσεις **cloudflared** περιγράφονται ξεχωριστά στο
> [`CLOUDFLARED.md`](CLOUDFLARED.md) — ο server τρέχει ήδη άλλες υπηρεσίες
> cloudflared, οπότε **δεν** στήνουμε νέο tunnel· προσθέτουμε ένα ακόμη hostname
> στο υπάρχον.

---

## 1. Γρήγορο στήσιμο με Docker Compose (χωρίς Coolify)

Ο πιο απλός τρόπος για να δουλέψει ο home server σήμερα:

```bash
git clone -b deploy/server https://github.com/scanmydata/mydata-etimologio-bridge.git
cd mydata-etimologio-bridge
cp .env.example .env      # συμπλήρωσέ το (δες §3)
docker compose up -d
docker compose logs -f etimologio
```

Έλεγχος: `curl http://localhost:8090/healthz.php` → `ok`

Το compose σηκώνει **Postgres 16** + την εφαρμογή, με named volumes για τα
δεδομένα. Δεν εκθέτει τίποτα στο internet — αυτό το κάνει το cloudflared.

---

## 2. Στήσιμο με Coolify

1. **Postgres:** *New Resource → Database → PostgreSQL*. Αντίγραψε το
   **internal connection URL** που δίνει (`postgres://user:pass@host:5432/db`).
2. **Εφαρμογή:** *New Resource → Application → Git repository*
   - Branch: **`deploy/server`**
   - Build Pack: **Dockerfile**
   - Port: **8090** (η μόνη θύρα που εκθέτει το image)

   > Όχι 8080: σε αυτόν τον server την κρατά **το ίδιο το Coolify**. Ο αριθμός
   > πρέπει να είναι ο ίδιος σε τρία σημεία — εδώ, στο `EXPOSE` του Dockerfile
   > και στο ingress του cloudflared.
3. **Volume (υποχρεωτικό):** mount στο **`/data`**.
4. **Env:** `DATABASE_URL` = το URL του βήματος 1, συν όσα θέλεις από τον πίνακα
   του §3. Το `DATABASE_URL` σπάει μόνο του σε DSN/χρήστη/κωδικό στο boot
   (`deploy/dburl.php`) — δεν χρειάζεται να το χωρίσεις με το χέρι, και δεν
   ορίζεις καθόλου `DB_DSN`.
5. Deploy. Στα logs θα δεις `[entrypoint] βάση από το connection string: pgsql:…`
   και `[entrypoint] η βάση απαντά`.

Ο container περιμένει μόνος του μέχρι **60 δευτερόλεπτα** να σηκωθεί η Postgres
πριν δεχτεί κίνηση, οπότε ένα ταυτόχρονο restart βάσης+εφαρμογής δεν αφήνει τους
πελάτες σε «500».

Το Coolify βάζει μόνο του reverse proxy· το cloudflared δείχνει σε αυτό.

---

## 3. Μεταβλητές περιβάλλοντος

Το `config.php` **παράγεται στο boot** από αυτές (`deploy/entrypoint.sh`), οπότε
το image δεν περιέχει κανένα μυστικό.

| Μεταβλητή | Υποχρεωτική | Σημείωση |
|---|---|---|
| `DATABASE_URL` | ✅ (Coolify) | `postgres://user:pass@host:5432/etimologio` — ό,τι δίνει το Coolify· αντικαθιστά τα τρία από κάτω |
| `DB_DSN` | ✅ (χωρίς URL) | `pgsql:host=postgres;port=5432;dbname=etimologio` |
| `DB_USER`, `DB_PASS` | ✅ (χωρίς URL) | στοιχεία Postgres |
| `MASTER_ADMIN_EMAIL` | ✅ | ο πρώτος διαχειριστής |
| `MASTER_ADMIN_PASSWORD` | 1η εκκίνηση | αποθηκεύεται **hashed**· **σβήσ' το μετά** |
| `APP_URL` | ✅ | δημόσιο URL, χωρίς `/` στο τέλος — μπαίνει στα emails |
| `SCHED_TOKEN` | για χρονοπρογραμματισμό | `openssl rand -hex 24`· κενό = απενεργοποιημένος |
| `RESEND_API_KEY`, `RESEND_EMAIL_SENDER` | για email | προτεινόμενο· το domain αποστολέα θέλει επαλήθευση |
| `SMTP_*`, `MAIL_PROVIDER` | προαιρετικά | εναλλακτική του Resend· `SMTP_SECURE` = `tls` (587) / `ssl` (465) / `none` |
| `NOTIFY_ADMIN_EMAIL` | προαιρετικό | `-` απενεργοποιεί τα email εκδόσεων |
| `ENCRYPTION_KEY` | προαιρετικό | base64 32 bytes. Αν οριστεί, **αντικαθιστά** το `/data/.enckey` — χρήσιμο για να ζει το κλειδί στα μυστικά του Coolify αντί για το volume. Ποτέ αλλαγή σε βάση με δεδομένα |
| `TZ` | προαιρετικό | `Europe/Athens` (default). Ο χρονοπρογραμματισμός δουλεύει σε ώρα Ελλάδας |
| `PHP_MEMORY_LIMIT`, `PHP_MAX_EXECUTION_TIME`, `PHP_UPLOAD_MAX_FILESIZE` | προαιρετικά | 512M / 300s / 32M· ανεβάζονται χωρίς rebuild για πολύ μεγάλες μαζικές εξαγωγές |

---

## 4. Το volume `/data` — μη το χάσεις

| Αρχείο | Τι είναι |
|---|---|
| `.enckey` | **το κλειδί κρυπτογράφησης**. Χωρίς αυτό τα αποθηκευμένα στοιχεία ΑΑΔΕ **δεν διαβάζονται ποτέ ξανά** |
| `.cookies/` | συνεδρίες ΑΑΔΕ ανά λογαριασμό |
| `.localdata.sqlite` | μόνο αν δεν έχεις ορίσει `DB_DSN` |

> ⚠️ Πάρε αντίγραφο του `.enckey` **χωριστά** από τη βάση. Η βάση χωρίς το κλειδί
> είναι άχρηστη, και το κλειδί χωρίς τη βάση επίσης — χρειάζονται και τα δύο.

---

## 5. Σύνδεση εταιρειών με τα κλειδιά ΑΑΔΕ

Ο server **δεν** έχει στοιχεία ΑΑΔΕ στο `config.php` (το `$ACCOUNTS` μένει
άδειο επίτηδες). Κάθε εταιρεία συνδέεται από την οθόνη **Διαχείριση**, και τα
διαπιστευτήριά της ζουν **κρυπτογραφημένα στη βάση**:

| Πεδίο | Πού το βρίσκει ο πελάτης | Πώς αποθηκεύεται |
|---|---|---|
| ΑΦΜ | — | καθαρό (χρειάζεται για φιλτράρισμα/scoping) |
| Ονομασία | ό,τι θέλει ο λογιστής | καθαρό |
| Username e‑Τιμολογίου | ο λογαριασμός του στο myDATA | **κρυπτογραφημένο** |
| Subscription key | ΑΑΔΕ → e‑Τιμολόγιο → Ρυθμίσεις → Στοιχεία Χρήστη | **κρυπτογραφημένο** |

**Η σειρά των βημάτων:**

1. Μπες ως master (`MASTER_ADMIN_EMAIL`) στο `https://<hostname>/app.php`.
2. **Διαχείριση → Χρήστες**: δημιούργησε/προσκάλεσε τον χρήστη της επιχείρησης
   (ρόλος `business`) ή τον λογιστή (ρόλος `editor`).
3. **Διαχείριση → Εταιρείες**: «Νέα εταιρεία» με ΑΦΜ, ονομασία, username και
   subscription key. Το κλειδί **δεν ξαναεμφανίζεται** ποτέ — η φόρμα δείχνει
   μόνο «έχει οριστεί». Κενό πεδίο σε επεξεργασία σημαίνει «κράτα το υπάρχον».
4. **Ανάθεση σε λογιστή** (0.4.1): ο `editor` βλέπει **μόνο** ό,τι του
   αναθέσεις. Χωρίς ανάθεση δεν βλέπει τίποτα.
5. Ο πελάτης μπαίνει με τα δικά του στοιχεία και δουλεύει τη δική του εταιρεία —
   ίδια βάση, ίδιο hostname, από browser ή από την εφαρμογή υπολογιστή.

Το κλειδί που κρυπτογραφεί όλα τα παραπάνω είναι το `/data/.enckey` (ή το
`ENCRYPTION_KEY` του περιβάλλοντος). **Χωρίς αυτό, τα subscription keys δεν
ξαναδιαβάζονται** και κάθε εταιρεία πρέπει να ξανασυνδεθεί από την αρχή.

Ο έλεγχος ότι όλη αυτή η αλυσίδα δουλεύει πάνω στην Postgres γίνεται με ένα
τρέξιμο (§6): το `tools/pg_smoke.php` καταχωρεί δοκιμαστική εταιρεία με κλειδιά,
τα ξαναδιαβάζει, επιβεβαιώνει ότι στη βάση είναι όντως κρυπτογραφημένα και μετά
τα σβήνει.

---

## 6. Έλεγχος ότι δουλεύει

**Ένα τρέξιμο τα ελέγχει όλα** — επεκτάσεις PHP, σύνδεση στην Postgres, όλους
τους πίνακες, την κρυπτογράφηση, τη σύνδεση εταιρείας με κλειδιά ΑΑΔΕ, πληρωμές,
cache, ειδοποιήσεις, χρονοπρογραμματισμό και το TLS προς την ΑΑΔΕ:

```bash
docker compose exec etimologio php tools/pg_smoke.php
```

Τελειώνει με «Ολα εντάξει — N έλεγχοι, καμία αποτυχία» (exit 0) και σβήνει μόνο
του ό,τι δημιούργησε. Σε Coolify: *Terminal* στο container, ίδια εντολή.

Χειροκίνητα, αν θέλεις τα επιμέρους:

```bash
curl -s http://localhost:8090/healthz.php                 # → ok (ζει η εφαρμογή)
curl -s "http://localhost:8090/healthz.php?db=1"          # → ok (απαντά και η βάση)
curl -s "http://localhost:8090/etimologio.php?auth=me"    # → JSON (χωρίς σύνδεση)
docker compose exec etimologio php -m | grep -E "pdo_pgsql|sodium|curl|zip"
docker compose exec postgres psql -U etimologio -d etimologio -c '\d account_managers'
```

**Δοκίμασε το email πριν προσκαλέσεις κόσμο:** Ρυθμίσεις → Πάροχος email →
«✉️ Δοκιμαστική αποστολή». Ο container πρέπει να μπορεί να βγει στην έξω θύρα
587 (ή 465) — σε πολλά VPS είναι κλειστή από τον πάροχο και τότε ο μόνος δρόμος
είναι το Resend (HTTPS).

Μετά, από browser: `https://<το hostname σου>/app.php` → οθόνη σύνδεσης.

**Σύνδεση της εφαρμογής υπολογιστή:** e‑Τιμολόγιο Pro → **Ρυθμίσεις** →
«Σύνδεση σε server» → βάλε το δημόσιο URL → **Σύνδεση σε server**.

---

## 7. Μεταφορά υπαρχόντων τοπικών δεδομένων (μία φορά)

Αν ένα γραφείο ξεκίνησε τοπικά (SQLite) και μετακομίζει στον server:

```bash
php tools/migrate_to_server.php \
  --from "/path/Παραστατικά myDATA/etimologio/local.sqlite" \
  --dsn "pgsql:host=postgres;dbname=etimologio" --user … --pass … --dry-run
```

Τρέξε πρώτα με `--dry-run`. **Αντίγραψε και το `.enckey`** στο volume, αλλιώς
όλα θα μεταφερθούν κρυπτογραφημένα και αδιάβαστα.

---

## 8. Τι ΔΕΝ είναι έτοιμο ακόμη

- **Πολλαπλά λογιστικά γραφεία στον ίδιο server.** Από την 0.4.1 ο ρόλος
  `editor` (λογιστής) βλέπει **μόνο** τις εταιρείες που του αναθέτει ο
  διαχειριστής — αυτό λύνει τον διαχωρισμό *μέσα* σε ένα γραφείο. Ο ρόλος
  `master` εξακολουθεί να βλέπει τα πάντα, οπότε για **δύο ανεξάρτητα γραφεία**
  στον ίδιο server χρειάζεται ακόμη απομόνωση με `tenant_id`: μέχρι τότε, **ένα
  instance ανά γραφείο**.
- **Αυτόματα backup σε Google Drive** — δεν έχει υλοποιηθεί· προς το παρόν
  χειροκίνητα (`pg_dump` + tarball του `/data`).
- **Infisical** — τα μυστικά μπαίνουν σήμερα ως απλά env στο Coolify.

---

## 9. Τι αλλάζει με την 0.4.1

- **Ανάθεση εταιρειών σε λογιστές.** Νέος πίνακας `account_managers`. Στην
  **πρώτη** εκκίνηση μετά την αναβάθμιση, κάθε υπάρχων λογιστής παίρνει ό,τι
  έβλεπε μέχρι τότε (δηλαδή τα πάντα) — αλλιώς θα κλειδωνόταν έξω. Πέρασε από
  **Διαχείριση → Χρήστες** και περιόρισε τις αναθέσεις· η ίδια οθόνη
  προειδοποιεί για λογιστές χωρίς καμία εταιρεία.
- **Το SMTP στέλνει πραγματικά.** Μέχρι την 0.4.0 τα πεδία `SMTP_HOST/USER/PASS`
  συλλέγονταν αλλά η αποστολή γινόταν με `mail()`, που στο image δεν έχει relay:
  **τίποτα δεν έφευγε, χωρίς μήνυμα λάθους**. Αν το γραφείο βασιζόταν σε SMTP,
  δες ξανά τις ρυθμίσεις και πάτα «Δοκιμαστική αποστολή».
- **Φωνή του βοηθού.** Οι μηχανές (Piper/whisper.cpp) **δεν** ταξιδεύουν στο
  image του server — είναι εκατοντάδες MB και δεν έχουν νόημα σε container. Τα
  `?tts=` / `?stt=` απαντούν `501` και το UI πέφτει πίσω στη φωνή του browser
  (ή ζητά γραπτή εντολή). Θέλεις φωνή στον server; Βάλε τα binaries σε volume
  και όρισε `PIPER_EXE` / `PIPER_VOICE_EL` / `WHISPER_EXE` / `WHISPER_MODEL`
  στο `config.php`.
- **Προτιμήσεις UI ανά χρήστη** (πλάτη/σειρά στηλών) ζουν στο `app_settings`,
  άρα ταξιδεύουν με τη βάση: ο ίδιος χρήστης βρίσκει τη διάταξή του και από
  άλλον υπολογιστή.

---

## 10. Τι κάνει το image για την έκθεση στο internet

Ο container κάθεται σε δημόσιο hostname, οπότε η εικόνα δεν είναι «PHP με τα
default»:

- **Καμία λεπτομέρεια σφάλματος στον χρήστη.** Το base image δεν έχει καθόλου
  `php.ini`, δηλαδή `display_errors=On`: ένα σφάλμα βάσης τύπωνε DSN και χρήστη
  μέσα στη σελίδα. Τώρα ισχύει το [`deploy/php.ini`](deploy/php.ini) — σφάλματα
  μόνο στα logs, και αν πέσει η βάση ο χρήστης βλέπει «προσωρινά εκτός
  λειτουργίας» (503) αντί για λευκή οθόνη.
- **Σερβίρονται μόνο όσα πρέπει.** Το [`deploy/apache-etimologio.conf`](deploy/apache-etimologio.conf)
  κόβει `config.php`, `.enckey`, `*.sqlite`, `tools/`, `deploy/`, τα `.md` και τα
  αρχεία βιβλιοθήκης· χωρίς λίστα καταλόγων· η ρίζα `/` πάει στο `app.php`.
- **Το web root δεν είναι εγγράψιμο** από τον χρήστη του Apache — ό,τι γράφεται
  πάει στο `/data`. Το lock του χρονοπρογραμματιστή μετακόμισε κι αυτό εκεί.
- **Cookie συνεδρίας** `HttpOnly` + `SameSite=Lax`, και `Secure` **μόνο** όταν το
  αίτημα ήρθε ως HTTPS (το βλέπει από το `X-Forwarded-Proto` του cloudflared) —
  αλλιώς θα έσπαγε η τοπική λειτουργία της εφαρμογής υπολογιστή στο 127.0.0.1.
- **Οι σύνδεσμοι των email βγαίνουν από το `APP_URL`**, όχι από την κεφαλίδα
  `Host` του αιτήματος: πλαστό `Host` σε αίτημα «ξέχασα τον κωδικό» δεν μπορεί
  να στείλει τον σύνδεσμο επαναφοράς σε ξένο domain.
- **Τα «εμπιστεύσου το loopback» μονοπάτια** (service‑auth του
  χρονοπρογραμματιστή, αυτόματο login της εφαρμογής υπολογιστή) απαιτούν πλέον
  και **απουσία κεφαλίδων proxy**, ώστε ένα `X-Forwarded-For: 127.0.0.1` από το
  internet να μη μοιάζει ποτέ με τοπικό αίτημα.
- **Το `pdo_pgsql` φορτώνει σίγουρα.** Το προηγούμενο `apt-get purge
  --auto-remove libpq-dev` έπαιρνε μαζί του και το `libpq5`, δηλαδή τη
  βιβλιοθήκη που χρειάζεται η ίδια η επέκταση· το build πλέον κρατά τις runtime
  βιβλιοθήκες και **αποτυγχάνει** αν λείπει `pdo_pgsql` ή `sodium`.

Ό,τι δεν κάνει το image το κάνει η Cloudflare: για **προστασία από επαναλαμβανό‑
μενες απόπειρες σύνδεσης** βάλε ένα Rate Limiting rule στο
`/etimologio.php` με `auth=login` (π.χ. 10 αιτήματα/λεπτό ανά IP). Μην βάλεις
Cloudflare **Access** μπροστά στο hostname — δες [`CLOUDFLARED.md`](CLOUDFLARED.md).

---

## 11. Live δοκιμή: σύνδεση εγκατάστασης γραφείου ↔ web server

Η εφαρμογή υπολογιστή μπορεί να δεθεί με αυτόν τον server με ένα **κλειδί
πρόσβασης**. Τότε δουλεύει ως **thin client**: τα δεδομένα ζουν στον server, και
ο πελάτης μπορεί να μπει από browser με έναν σύνδεσμο, χωρίς εγκατάσταση.

### Α. Ο server (μία φορά)

1. Deploy στο Coolify όπως στο §2 — Dockerfile, θύρα **8090**, volume `/data`,
   `DATABASE_URL` από την Postgres του Coolify.
2. Πρόσθεσε το hostname στο υπάρχον tunnel ([`CLOUDFLARED.md`](CLOUDFLARED.md)).
3. Βάλε το τελικό `APP_URL` (π.χ. `https://etimologiopro.scanmydata.gr`) και redeploy.
   **Το `APP_URL` μπαίνει μέσα στο κλειδί** — αν είναι λάθος, η εφαρμογή
   υπολογιστή θα ψάχνει λάθος διεύθυνση.
4. Έλεγχος πριν προχωρήσεις:

```bash
curl -s https://etimologiopro.scanmydata.gr/healthz.php?db=1
```

   Πρέπει να απαντήσει `ok`. Μετά, μέσα από τον container:

```bash
docker compose exec etimologio php tools/pg_smoke.php
```

### Β. Το κλειδί (στον server, από browser)

1. `https://<hostname>/app.php` → σύνδεση ως **master**.
2. **Διαχείριση**: φτιάξε τον χρήστη του γραφείου/πελάτη και **ενεργοποίησέ** τον·
   πρόσθεσε τις εταιρείες του (ΑΦΜ, username, subscription key).
3. **Ρυθμίσεις → «🔑 Κλειδιά πρόσβασης — σύνδεση εφαρμογής υπολογιστή»** →
   «➕ Νέο κλειδί» → **αντίγραψέ το τώρα** (εμφανίζεται μία μόνο φορά).
   Το κλειδί έχει τη μορφή `etim1_…` και **κουβαλά μέσα του τη διεύθυνση**.

### Γ. Η εφαρμογή υπολογιστή

**e‑Τιμολόγιο Pro → Ρυθμίσεις → «☁️ Σύνδεση με web server»**: επικόλληση κλειδιού
→ «Σύνδεση σε server». Η ίδια οθόνη δείχνει μετά τις εταιρείες σου και τον
**σύνδεσμο που δίνεις στον πελάτη**.

> Από την 0.4.7 όλες οι ρυθμίσεις του e‑Τιμολόγιο ζουν **μέσα** στο e‑Τιμολόγιο.
> Ο Πίνακας ελέγχου του Downloader δεν έχει πια κουτί «Σύνδεση σε server» ούτε
> αντίγραφα ασφαλείας — ήταν ρυθμίσεις άλλης εφαρμογής σε λάθος οθόνη.

Η ρύθμιση γράφεται στο `service.json` της εγκατάστασης, οπότε **κλείσε και
ξανάνοιξε την εφαρμογή** για να δουλέψει πάνω στον server.

### Δ. Επαλήθευση

- Στην εφαρμογή, η κάρτα δείχνει «συνδεδεμένο», τη διεύθυνση και τον λογαριασμό
  που αναγνώρισε το κλειδί.
- Δώσε τον σύνδεσμο `https://…/app.php` στον πελάτη με τα δικά του στοιχεία
  εισόδου: βλέπει τα ίδια δεδομένα.
- Στα «Κλειδιά πρόσβασης» του server, η στήλη «Τελευταία χρήση» γεμίζει.

### Ε. Αμφίδρομος συγχρονισμός

Το **«🔄 Συγχρονισμός τώρα»** (ίδια κάρτα) δουλεύει **και προς τις δύο
κατευθύνσεις** — δεν υπάρχει «σωστή» πλευρά:

| | |
|---|---|
| Τι ανεβαίνει/κατεβαίνει | **πληρωμές** και **καρτέλες πελατών** (υπόλοιπο ανοίγματος, σημειώσεις)· την πρώτη φορά και η **εταιρεία με τα διαπιστευτήρια ΑΑΔΕ**, αν λείπει από τον server |
| Πώς αναγνωρίζονται | κάθε πληρωμή από το **περιεχόμενό** της (πελάτης+ημερομηνία+ποσό+ΜΑΡΚ+σημείωση) — δεύτερος συγχρονισμός δεν διπλογράφει τίποτα |
| Σύγκρουση σε καρτέλα | κερδίζει η **νεότερη** εγγραφή· ίδιο περιεχόμενο = καμία εγγραφή |
| Παραστατικά | **δεν** ταξιδεύουν — και οι δύο πλευρές τα διαβάζουν από την ΑΑΔΕ με τα ίδια διαπιστευτήρια |
| Διαγραφές | **δεν** ταξιδεύουν. Χωρίς ταφόπλακες, μια διαγραφή στη μία πλευρά θα ξαναγύριζε από την άλλη· σβήσε και στις δύο |

Επαληθεύτηκε με δύο instances: πληρωμή που γράφτηκε στο γραφείο βρέθηκε στον
server, πληρωμή που γράφτηκε στο web βρέθηκε στο γραφείο, και ο τρίτος
συγχρονισμός δεν άλλαξε τίποτα (`sent 0 / recv 0`).

### ΣΤ. Τι να προσέξεις

- **Μαζική μεταφορά παλιάς βάσης**: για γραφείο με χρόνια δεδομένων, ο πρώτος
  συγχρονισμός στέλνει τα πάντα σε ένα request. Αν είναι τεράστια, προτίμησε το
  `tools/migrate_to_server.php` (§7) και άσε τον συγχρονισμό για τη συνέχεια.
- **Ανάκληση κλειδιού** (Ρυθμίσεις → Κλειδιά πρόσβασης) κόβει αμέσως τη σύνδεση
  εκείνης της εγκατάστασης.
- Το κλειδί είναι **διαπιστευτήριο**: στείλ' το όπως θα έστελνες κωδικό.
